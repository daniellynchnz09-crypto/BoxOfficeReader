@AGENTS.md

# Box Office Top 25

Next.js frontend for an n8n-powered box office tracker. See [README.md](README.md) for stack/setup.

## Architecture

- **`Box Office Mojo Top 25 Tracker`** (n8n, scheduled daily 7am) — scrapes Box Office Mojo, cross-references the `box office test` Google Sheet, enriches new entrants via OMDb + Gemini, caches static per-film data (poster, fun fact, plot, director, cast) in two n8n Data Tables (`box_office_film_cache`, `box_office_film_details`), and updates a Google Slides deck. This is the expensive, heavy workflow — never call it from the frontend.
- **`Box Office Top 25 API`** (n8n, on-demand webhooks) — the frontend's actual data source. Two GET endpoints:
  - `/webhook/box-office-top25` — the full ranked list (reads Sheet1 + cache table only, fast)
  - `/webhook/box-office-film?name=<film name>` — single film detail (reads Sheet1 + both cache tables)

The frontend never triggers the daily scraper directly — it only reads already-computed data via the API workflow, which is why list loads are fast (~3s) despite the daily workflow being slow (scraping + OMDb + AI + Slides).

Both `Box Office Top 25 API` webhooks require an `X-Webhook-Secret` header (n8n `httpHeaderAuth` credential "Box Office API Webhook Secret"); `lib/n8n.ts` sends it from `N8N_WEBHOOK_SECRET`. Both endpoints also return a generic `{ error, message }` JSON body with a 500 status on any read failure (Google Sheets/Data Table errors are handled via `onError: continueErrorOutput` fanning into a shared "Build Error Response" → "Respond Error" pair per branch) instead of leaking n8n's default error page.

## Weekly trends (Sheet2) — live as of Aug 18

`Sheet2` in the same Google Sheet is a wide/pivot table: one row per "Week in Theatres" (1, 2, 3…), one column per film name, cell = that film's cumulative worldwide gross at that many weeks in release. A blank cell (sometimes an actually-missing key, since Google Sheets drops trailing blank cells from a row rather than sending `""`) means "hasn't reached that many weeks yet" — treat both the same, as `null`.

The read-replica lag that used to make `Read Sheet2` inconsistent (from the earlier ~19k-row cleanup) tested clean on Aug 18 — 6 reads over ~2.5 min, all 1–2.6s, no outliers. `Read Sheet2` is now wired into `Box Office Top 25 API`'s `Get Top 25` branch (`Read Sheet1` fans out to `Read Cache` + `Read Sheet2`, both merge into `Build Top 25`, which computes `weeks`/`weeklyGross`). **`Read Sheet2` has `executeOnce: true` set** — without it, a Google Sheets "get many" node re-runs once per upstream input item rather than once total, so it read the sheet 27× and appended every copy (26 rows → 702). This was already happening to `Read Cache` too but stayed invisible there because `Build Top 25` dedups cache rows into a lookup object keyed by film name; it's not safe to remove `executeOnce` from either node.

Both endpoints are wired up as of Aug 18: the list endpoint feeds `components/FilmCard.tsx`'s small `Sparkline`, and `Get Film Detail` has its own separate `Read Sheet2 Detail` node (same `executeOnce: true` requirement) merged into `Merge Detail Reads`, feeding `components/TrendChart.tsx` — a bigger version with dashed-gridline axes and $ /week labels, rendered on `app/movie/[name]/page.tsx` between the fun fact and the IMDb link.

## Conventions

- Never commit secrets or webhook URLs with auth tokens — use `.env.local` locally and Vercel's env var settings in production.
- Prefer editing the n8n workflows via the n8n-mcp MCP server over asking the user to do it manually in the n8n UI.
- When editing n8n workflows via `n8n_update_partial_workflow`, pass `operations` as its own top-level parameter (not nested inside a wrapper object) — large combined operation arrays have intermittently failed to parse when constructed as one big embedded JSON blob.
- This local folder is **not** a git repository (no `.git`, and `git` isn't installed on this machine), but Vercel deploys from the connected GitHub repo `daniellynchnz09-crypto/BoxOfficeReader` (`main` branch) whenever it changes. Local file edits do nothing for the deployed site on their own. **After making any code change here, push the changed files to that repo via the GitHub MCP server** (it works over the API, no local git needed) — otherwise Vercel keeps serving stale code, which has already caused a real outage (see the 403/webhook-auth incident in git history — the fix was written locally but never deployed for days). Never push `.env.local` itself.
- Exception to the above: edits to `CLAUDE.md` itself don't need to be pushed to GitHub right away. It doesn't affect the deployed site (Vercel builds the app, not this file), so it's fine for it to only exist locally for a while and go out later, batched with the next real code push.
- Playwright MCP is available at user scope (confirmed working Aug 18) — use it to navigate the deployed site, take screenshots, resize the viewport, and inspect computed styles/pixel data rather than reasoning from CSS/code alone. Note its browser is desktop Chromium even when the viewport is resized to mobile dimensions (`navigator.userAgent` reports desktop Chrome, no touch/WebKit emulation) — it can confirm CSS logic is correct at a given viewport width, but can't reproduce iOS-Safari-specific engine quirks. Don't overclaim a mobile-Safari-specific bug as fixed based on this tool alone; say so explicitly and ask the user to confirm on a real device.
- If a Next.js dev-server response looks frozen/stale despite source code and the upstream API both being confirmed correct, clearing `.next/cache` alone may not be enough — Turbopack's dev cache lives at **`.next/dev/cache`**, a different path. Delete the whole `.next` folder and restart to be sure.

## TODO — next session

### Bugs / requests — Aug 18 follow-up

1. **"Obsession" poster faded look — investigated with Playwright MCP (now available at user scope), root cause confirmed, no fix needed.** Compared `next/image`'s resize behavior directly: downscaling to thumbnail size (w=96, q=75) reduces contrast (std-dev of luminance) by ~11% for both Obsession and Pegasus 3 — proportionally identical, so the resize pipeline isn't singling this poster out, and there's no CSS filter/opacity/blend-mode difference applied to it either. The actual cause is the source image itself: Obsession's raw poster has mean luminance 39.6 (dark, moody, low-key) vs Pegasus 3's 116.4 (bright, saturated). At a 56-96px thumbnail with q=75 compression, a dark low-key composition reads as hazy/muddy while a bright poster stays crisp — that's inherent to this poster's content, not a defect in the pipeline. Confirms last session's finding that the raw file is clean. **No fix planned** — per the standing caution not to guess-fix without confirming cause, and now that the cause is confirmed to be content-driven rather than a pipeline bug, a blanket fix (e.g. per-image contrast boost) isn't warranted.
2. **Homepage ambient gradient on mobile — mechanism confirmed correct, but not fully verified on real iOS Safari.** Checked the `body::before` fixed-position glow (from the `background-attachment: fixed` → `position: fixed` rework) via Playwright MCP at a 390×844 mobile viewport: computed styles show `position: fixed`, correct gradient stops, opacity 1, z-index -1, all applied and visibly rendering (faintly, as intended after the 70% opacity cut). This confirms the CSS logic itself is correct in a standards-compliant engine. **Caveat: the Playwright MCP session here runs desktop Chromium resized to a mobile viewport** (`navigator.userAgent` confirms — no real WebKit/mobile-Safari engine, no touch emulation), so this rules out a general logic bug but can't rule out an iOS-Safari-specific quirk, which is exactly the class of bug a resized desktop browser can't reproduce. If the user still doesn't see it on an actual iPhone, treat the fix as unconfirmed for that case and investigate further.

### Resolved this session (verify visually next time you're on a build)

- Movie detail pages got a bigger `TrendChart` (dashed gridlines, $ y-axis, week x-axis, gold fill under the line) between the fun fact and the IMDb link, sourced from a second, separate `Read Sheet2 Detail` wired into `Get Film Detail`'s branch. Spent a while chasing a false lead here: the chart kept showing "Not enough weeks tracked" even after confirming (via a direct webhook call bypassing the browser entirely) that the API was returning real `weeks`/`weeklyGross`. Root cause was a stale Turbopack dev cache at `.next/dev/cache` — a **different path** from the `.next/cache` fetch-cache directory that's more commonly documented; clearing `.next/cache` alone didn't fix it, only deleting the whole `.next` folder did. Worth remembering next time a dev-server response looks frozen despite source and upstream both being correct.
- Weekly trends (Sheet2) re-enabled — see "Weekly trends (Sheet2) — live as of Aug 18" above for the n8n-side wiring/`executeOnce` fix. There was also a **separate, pre-existing frontend bug** found while verifying: `lib/n8n.ts`'s `withWeeklyDefaults()` unconditionally overwrote every film's `weeklyGross` with `[]` (`films: data.films.map((f) => ({ ...f, weeklyGross: [] }))`) regardless of what the API sent — harmless while the API never sent `weeklyGross` at all, but it silently discarded the real per-film data the moment the n8n side started sending it. Fixed to `f.weeklyGross ?? []`. Caught by testing the live webhook directly (got real weekly arrays back) and then testing the actual homepage in a browser (still showed "Not enough weeks tracked yet" for every film) — the mismatch between those two results is what surfaced it. Worth remembering: a correct backend response doesn't guarantee the frontend renders it — check both ends independently.
- Expand-glow moved from the film title to the rank number: `components/FilmCard.tsx`'s `<Link>` no longer gets a glow class; the `.ticket-stub` wrapper does instead (via the renamed `.rank-glow` class in `app/globals.css`, was `.title-glow`).
- Movie detail page load times: raised `getFilmDetail()`'s fetch `revalidate` from 300s to `21600` (6h) in `lib/n8n.ts`. This part is live and safe.
  - **Reverted: `generateStaticParams()` on `app/movie/[name]/page.tsx` broke every detail page in production and has been removed.** What happened: it returned `encodeURIComponent(film.name)` (e.g. `Spider-Man%3A%20Brand%20New%20Day`) because returning the raw name crashed the local Windows build (`:` isn't legal in a Windows path, and `next build` writes a real directory per prerendered route). But Next.js encodes the param value *again* when building the actual served route, so real visits — which arrive already singly-encoded via the homepage's `Link href` — got matched to a doubly-encoded prerendered page. `decodeURIComponent()` only unwraps one layer, so `name` stayed literally as `Spider-Man%3A%20Brand%20New%20Day`, matched no film, and every detail page showed "isn't in the current Top 25." Confirmed and fixed by removing `generateStaticParams()` and the page-level `revalidate` export entirely; the route is back to `ƒ` fully-dynamic per-request rendering, still benefiting from the 6h fetch-level cache above.
  - If someone wants to re-attempt full prerendering later: `generateStaticParams()` needs to return the **raw, unencoded** film name (the Next.js-standard convention — Next handles the URL encoding itself). That's correct for Vercel's Linux build servers (colons are legal in Linux filenames) but **cannot be verified with `npm run build` on this Windows dev machine** for any title containing a colon (there's currently at least one, "Spider-Man: Brand New Day") — it will crash locally with an `ENOENT`/`mkdir` error even though it would build fine on Vercel. Test it by watching the actual Vercel build log after pushing, not by trusting a local Windows build to catch a problem it structurally can't reproduce.
- Movie detail page background darkness: user asked for a 30% reduction as a first pass, to assess and iterate from there. Applied a uniform 30% relative reduction to the dark overlay gradient's opacity stops in `app/movie/[name]/page.tsx` (`from-bg/60 via-bg/80 to-bg` → `from-bg/42 via-bg/56 to-bg/70`); the poster `<Image>`'s own `opacity-35` was left untouched since the user's instruction was one single lever, not two. Net visible-poster math with the new values (poster opacity still 35%): ~20% visible at the top (was ~14%), ~15% at the middle (was ~7%), ~11% at the bottom (was 0% — previously the bottom stop was fully opaque `to-bg`, hiding the poster completely; now it stays faintly visible even there). **Waiting on user feedback** — if they want it lighter/darker still, adjust these same three `from/via/to` percentages (and/or `opacity-35` on the `<Image>` if the poster itself should also brighten).

### Design / UX backlog — Sheet2-sourced

The Sheet2 blocker is resolved; the homepage `Sparkline` and the movie detail page's bigger `TrendChart` (with axes) both work now — see "Weekly trends (Sheet2) — live as of Aug 18" above. Still not built:

1. A chart on the homepage (`app/page.tsx`) showing all 25 entries' trajectories together on one multi-line chart, similar to the one already generated from Sheet2 for the Google Slides deck. `lib/chart.ts`'s `CHART_PALETTE` (8 named colors) exists for this but is currently unused — it was added ahead of this feature.
